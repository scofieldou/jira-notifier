# 安裝與設定教學

從零開始到收到第一則通知。全程約三分鐘，不需要編輯任何檔案，也不需要安裝執行環境或套件。

功能總覽與設計說明見 [README.md](README.md)。

## 事前準備

- **Chrome 或 Edge**（以下以 Chrome 為例，Edge 步驟相同，網址把 `chrome://` 換成 `edge://`）
- **一個自架的 Jira Server**，而且你在這個瀏覽器上已經登入
- Git（或直接下載 zip 也行）

### 先確認你是自架 Jira

瀏覽器打開 Jira，看網址列：

- `https://你的公司.atlassian.net` → 這是 **Jira Cloud，本擴充功能不支援**
- 其他任何位址（常見形式為 `http://jira.公司網域:8080`）→ 自架，可以繼續

## 步驟 1：取得程式碼

```bash
git clone <這個 repo 的網址> jira-notifier
```

⚠️ **放在一個不會被移動或刪除的位置。** Chrome 是直接讀取這個資料夾，不是把內容複製走——資料夾一旦搬走或刪掉，擴充功能就會失效。

## 步驟 2：載入到 Chrome

1. 網址列輸入 `chrome://extensions`
2. 開啟右上角的**開發人員模式**
3. 點左上角**載入未封裝項目**
4. 選擇 `jira-notifier` 資料夾（選資料夾本身，不是裡面的檔案）

工具列會出現一個藍底白 `J` 的圖示。看不到的話點工具列的拼圖圖示，把它釘選出來。

此時擴充功能還不會運作，因為尚未設定 Jira 位址。

## 步驟 3：設定 Jira 位址

點擴充功能圖示，popup 會顯示「尚未設定 Jira 位址」，點**開啟設定**。也可以從 `chrome://extensions` 的**擴充功能選項**進入。

### 找出你的站台位址

登入 Jira 後看網址列，取到**連接埠為止**、不含後面的路徑。例如網址是：

```
http://jira.mycompany.com:8080/secure/Dashboard.jspa
```

那要填的就是：

```
http://jira.mycompany.com:8080
```

結尾的斜線和後面的路徑不用管，填進去也會自動去掉。

### 授權

填好後按**測試並儲存**。Chrome 會跳出對話框，詢問是否允許本擴充功能「讀取和變更你在該網站上的資料」——**必須選擇允許**，否則擴充功能讀不到 Jira。

這個授權是動態的，只涵蓋你填入的那一個網域。之後換位址時，舊網域的權限會自動收回。

### 看結果

授權後會立刻實際連一次 Jira，結果顯示在輸入框下方：

| 訊息 | 意思 |
| --- | --- |
| 已連線：你的名字 | 全部正確，設定完成 |
| 位址正確，但這個瀏覽器尚未登入 Jira | 位址對了，去登入 Jira 即可，設定已存檔 |
| 連不上這個位址 | 位址打錯，或不在公司網路環境 |
| 回應不是 JSON | 這個位址不是 Jira 的 REST 端點，確認是否填錯站台 |

## 步驟 4：確認運作

**看徽章。** 圖示右下角應該在幾秒內出現一個藍色數字，那是目前指派給你、尚未結案的單量。數字是 0 時不顯示徽章。

| 徽章 | 意思 | 處理 |
| --- | --- | --- |
| 藍色數字 | 正常 | — |
| 紅色 `!` | Jira 未登入或 session 過期 | 開啟 Jira 登入即可 |
| 橘色 `?` | 位址錯誤、權限被撤銷或連不上 | 點圖示看 popup 的說明 |

**看清單。** 點圖示打開 popup，應該列出你的單，依專案分組。

**測通知。** 通知只在**出現新單號**時才跳，所以剛裝好不會有任何通知（見下一節）。要立刻確認通知管道暢通，到設定頁的「通知」區塊按**送出測試通知**——桌面右下角應該立刻跳出一則假通知。

這則測試通知不受通知門檻限制，一定會跳；跳不出來就代表問題在作業系統那端，見 README 的疑難排解一節，多半是 Windows 的專注輔助擋掉了。

## 步驟 5：其他設定

設定頁還有三項，改完即時生效，不需要重新載入擴充功能：

- **檢查間隔** —— 背景多久輪詢一次，預設 2 分鐘。這也決定了被指派後最久多久收到通知
- **通知門檻** —— 只有某個優先度以上才跳通知。只影響通知，清單與徽章一律顯示全部
- **通知停留在畫面上直到我點掉** —— 取消勾選的話通知會在數秒後自動消失並收進重要訊息中心

## 首次執行的行為

**裝好之後不會馬上有通知，這是刻意的。**

擴充功能會記住「目前看過哪些單號」作為基準，只有清單裡出現**新的**單號才通知。如果第一次執行就通知，你會被既有的十幾張單一次洗版。

所以安裝當下它只會靜靜建立基準。下一次有人指派新的單給你，才會跳通知。

## 日常使用

裝好之後不需要做任何事。瀏覽器開著它就在背景輪詢。

- **收到通知**：點通知直接開啟該單
- **看待辦清單**：點工具列圖示，點專案標題可收合該組
- **改設定**：popup 右上角的齒輪
- **登入過期**：徽章變紅色 `!` 時，開啟 Jira 重新登入，下次輪詢自動恢復

## 更新版本

```bash
git pull
```

然後到 `chrome://extensions` 點本擴充功能卡片上的**重新整理**（↻）。

所有設定都存在 `chrome.storage` 裡，不會被 `git pull` 覆蓋，更新後不需要重新設定。

改動程式碼後也要按一次 ↻ 才會生效，這點在開發時特別容易忘。

## 常見問題

**載入時出現 "Manifest file is missing or unreadable"**
選錯資料夾了。要選的是**包含 `manifest.json` 的那一層**，不是它的上層目錄。

**設定頁按「測試並儲存」沒有跳出授權對話框**
表示該網域的權限先前已經授予過了，這是正常的，直接看測試結果即可。

**徽章顯示橘色 `?`，popup 說「缺少存取權限」**
權限被撤銷了（可能是在 `chrome://extensions` 的詳細資料裡手動移除）。回設定頁再按一次「測試並儲存」重新授權。

**徽章顯示紅色 `!` 但我明明登入了**
確認是**同一個瀏覽器設定檔**。在 Edge 登入不會讓 Chrome 的擴充功能拿到 session。另外檢查是否用了無痕視窗登入。

**清單是空的但我確實有被指派的單**
目前的查詢條件是「指派給我**且未結案**」。你的單如果都已經是 Resolved 或 Closed，清單就會是空的，這是正確行為。

**Chrome 每次啟動都問要不要停用開發人員模式擴充功能**
這是未上架 Chrome 線上應用程式商店的擴充功能的正常提醒，選擇保留即可。
